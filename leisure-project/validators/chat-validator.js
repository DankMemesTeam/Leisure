const isValidPrivateChat = (chatModel) => {
    return true;
};

const isValidEventChat = (chatModel) => {
    return true;
};

const isValidMessage = (messageModel) => {
    return true;
};

module.exports = {
    isValidPrivateChat,
    isValidEventChat,
    isValidMessage,
};
