const isValid = (articleModel) => {
    return true;
};

const isValidEdit = (id, title, description, content) => {
    return true;
};

const isValidComment = (articleId, comment) => {
    return true;
};

module.exports = {
    isValid,
    isValidEdit,
    isValidComment,
};
