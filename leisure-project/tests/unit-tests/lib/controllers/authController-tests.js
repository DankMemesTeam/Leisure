const { expect } = require('chai');
const sinon = require('sinon');

// ({ articleData, categoryData, userData })
const articleController = require('../../../../lib/controllers/auth-controller');

describe('AuthController tests', () => {

});