module.exports = () => {
    // needs better validation
    return {
        isUserValid(user) {
            // add validations using validator library

            return true;
        },
        // isPostValid(post) etc...
    };
};
